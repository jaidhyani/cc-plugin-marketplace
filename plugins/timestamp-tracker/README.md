# timestamp-tracker

Automatically tracks timestamps of user prompts and Claude responses to provide temporal context within sessions.

This plugin works entirely through hooks — no manual commands or configuration needed. It captures timing data as you interact with Claude Code, making timestamps available for downstream tools and analysis.
