# pr-watch

Block until PRs change, then report what happened.

## Usage

Use the `/pr-watch` command to monitor pull requests. The plugin polls your configured repositories and blocks execution until it detects changes, then reports what happened (new PRs, status changes, comments, etc).
